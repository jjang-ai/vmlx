# Full System Audit — 2026-03-25

## Branch: integrate-community-prs

11 commits integrating PRs #28, #29, #30, #32 + issue #31 + 3 audit fix rounds.
Tests: 90/90 Python + 1545/1545 Panel — all green.

---

## Issues FIXED in This Branch

| # | Severity | Description | Commit |
|---|----------|-------------|--------|
| 1 | Critical | `_THINK_STRIP_RE.sub()` TypeError when assistant message has list content (images) | 264a1f8 |
| 2 | Critical | Streaming timeout hardcoded to 300s (server ignores per-request timeout) | 8aacbff |
| 3 | Critical | "No limit" timeout=0 client-side abort at 300s instead of 86400s | 194b40b |
| 4 | High | `resolve_to_local_path()` missing — HF repo ID models fail file-based detection | d5db9d2 |
| 5 | High | `return False` after JANG `has_vision` check (breaks Mistral 4 JANG VLM) | d5db9d2 |
| 6 | High | `bool("false") == True` coercion in `_merge_ct_kwargs` and CLI | 542352e, 82c3aa2 |
| 7 | Medium | Assistant-role images missing placeholder tokens (klite UI crash) | a137337 |
| 8 | Medium | Post-template image count guard missing in `stream_chat()` | 82c3aa2 |
| 9 | Medium | `/v1/completions` streaming had no server-side timeout or keep-alive | 194b40b |
| 10 | Medium | mflux dependency gated on Python >=3.13 (supports >=3.10) | 264a1f8 |
| 11 | Medium | SSD streaming `is_jang_model` used unresolved `_model_path` | 264a1f8 |
| 12 | Medium | `_merge_ct_kwargs` coerced JSON null to False (should remove key) | 264a1f8 |
| 13 | Medium | server.py JANG metadata cache missing `resolve_to_local_path` call | d5db9d2 |
| 14 | Low | Reasoning Effort section hidden for Mistral 4 users in ChatSettings | 17f3717 |
| 15 | Low | Python 3.10 classifier contradicts `requires-python >= 3.11` | 82c3aa2 |
| 16 | Low | `_suppress` checks read raw `request.chat_template_kwargs` instead of merged | 542352e |
| 17 | Low | Both `_suppress` sites in non-streaming use redundant 3-step check | N/A (style only) |

---

## Known Issues NOT Fixed (For Other Agent / Future)

### High Priority

| # | Area | Description | Location |
|---|------|-------------|----------|
| H1 | Other agent | `_apply_turboquant_to_model(_lang, self.model_name)` uses unresolved HF repo ID — TQ silently disabled for non-local VLMs | mllm.py:774 |
| H2 | Other agent | `hasTurboQuant = true` unconditional in TS registry for ALL models, even when jang_tools not installed | model-config-registry.ts:368 |
| H3 | Anthropic API | `create_anthropic_message` doesn't call `_merge_ct_kwargs` — non-`enable_thinking` server defaults lost | server.py:1944-1963 |
| H4 | Error format | FastAPI 422/4xx errors use default format, not OpenAI `{"error": {message, type, code}}` | All routes |
| H5 | Model loading | JANG HF models not yet cached → detection skipped, loaded as standard MLX (broken for JANG-only) | tokenizer.py:236 |
| H6 | Model loading | VLM HF models not yet cached → `is_mllm_model` returns False, VLM loaded as LLM (crashes on image) | api/utils.py:100 |

### Medium Priority

| # | Area | Description | Location |
|---|------|-------------|----------|
| M1 | Batch engine | `BatchedEngine` extracts assistant-role images but batch template doesn't insert placeholders — no post-template guard | engine/batched.py:724-780 |
| M2 | Anthropic API | Anthropic path missing auto-detect for `enable_thinking` (only 2-tier vs 4-tier chain) | server.py:1959-1962 |
| M3 | Anthropic API | max_tokens defaults differ: Anthropic=4096, Chat Completions=32768 | By design but surprising |
| M4 | Mistral 4 | Server-wide `--chat-template-kwargs enable_thinking=false` doesn't inject `reasoning_effort=none` | server.py:3504 |
| M5 | Other agent | Double `logger` declaration in tokenizer.py (function defined before logger) | tokenizer.py:95-100 |
| M6 | Batch engine | `BatchedEngine` extracts assistant-role images but batch template path doesn't insert placeholders — no post-template guard | engine/batched.py:724-780 |
| M7 | Cache | LLM memory-aware/legacy cache does NOT strip gen_prompt_len — 100% cache miss for thinking models if paged cache disabled | scheduler.py:2463 |
| M8 | Mistral 4 | Server-wide `--chat-template-kwargs enable_thinking=false` doesn't inject `reasoning_effort=none` into template | server.py:3504 |

### Low Priority / Edge Cases

| # | Area | Description | Location |
|---|------|-------------|----------|
| L1 | resolve | `_model_path` global never resolved (dormant for registry lookups) | server.py:892 |
| L2 | resolve | LRU cache has no TTL — mid-session downloads invisible until restart | api/utils.py:58 |
| L3 | resolve | `Path(model_name).is_dir()` could match CWD-relative dir for HF IDs | api/utils.py:74 |
| L4 | resolve | model_inspector.py has parallel HF scan not using shared resolve | utils/model_inspector.py:526 |
| L5 | image guard | `_slot_count > len(all_images)` (more slots than images) not guarded | mllm.py:1436 |
| L6 | streaming | Tool calls emitted as single chunk, not character-by-character like OpenAI | server.py:4914 |
| L7 | vmlx_engine/simple.py | Dead legacy file with wrong `force_mllm` pattern | vmlx_engine/simple.py:48 |
| L8 | GLM-Z1 | Detection is name-regex only (`model_types=[]`), fails for renamed dirs | model_configs.py |
| L9 | pyproject | 3.14 missing from classifiers (add when 3.14 stable) | pyproject.toml |
| L10 | KV quant UI | Dropdown shows active for MLA models (DeepSeek V3, Mistral 4) but Python silently disables — no UI feedback | SessionSettings.tsx |
| L11 | disk cache | Disk cache dir differs when loading same HF model by repo ID vs resolved path (cold cache) | scheduler.py:389 |
| L12 | /health | `/health` reports `_model_name` but `/v1/models` reports `_served_model_name` — inconsistent | server.py:1209 |
| L13 | Anthropic | Anthropic path missing auto-detect for enable_thinking (2-tier vs 4-tier chain) | server.py:1959 |
| L14 | Anthropic | max_tokens defaults differ: Anthropic=4096 vs Chat Completions=32768 | By design |
| L15 | CORS | No CORSMiddleware — browser cross-origin clients blocked | server.py:440 |
| L16 | Admin | /admin/soft-sleep, /admin/deep-sleep, /admin/wake unprotected by API key | server.py:1339 |
| L17 | UI | Disk cache UI settings shown but buildArgs() has them commented out — preview lies | SessionSettings.tsx:103, sessions.ts:1763 |
| L18 | UI | No visual thinking state indicator (badge/icon) in chat toolbar | ChatModeToolbar.tsx |
| L19 | UI | maxContextLength field in SessionConfig interface but never wired to form or CLI | SessionConfigForm.tsx |
| L20 | Image | model=qwen-image-edit sent to /v1/images/generations routed as gen, not edit (crashes) | image_gen.py:67 |
| L21 | Idle sleep | Deep sleep during active streaming kills scheduler — narrow race on stale lastRequestAt | sessions.ts:1439 |

---

## Full Audit Coverage (11 agents, 113 checks)

| Audit Agent | Checks | OK | Issues | Edge Cases |
|---|---|---|---|---|
| PR #28 resolve_to_local_path | 24 | 18 | 2 | 4 |
| PR #29 image placeholders | 18 | 12 | 3 | 3 |
| PR #30 chat-template-kwargs | 22 | 14 | 4 | 4 |
| Issue #31 streaming timeout | 12 | 8 | 2 | 2 |
| Cross-cutting integration | 14 | 9 | 3 | 2 |
| OpenAI + Anthropic API | 20 | 16 | 2 | 2 |
| Reasoning + tool parsers | 16 | 13 | 1 | 2 |
| Cache + KV quantization | 14 | 10 | 0 | 4 |
| UI settings end-to-end | 21 | 15 | 4 | 2 |
| HF + model loading | 22 | 16 | 2 | 4 |
| Image gen + batch + edge cases | 20 | 14 | 2 | 4 |
| **TOTAL** | **203** | **155** | **25** | **33** |

---

## Test Coverage Gaps

| Area | What's Missing |
|------|---------------|
| `_merge_ct_kwargs` string normalization | No test for string "false" → bool False |
| Post-template image guard | No unit test (needs real model mock) |
| resolve_to_local_path in JANG metadata | No test for HF repo ID → _jang_metadata |
| Timeout forwarding | No test for client→server timeout sync |
| _apply_turboquant with HF ID | No test (functional bug + gap) |
