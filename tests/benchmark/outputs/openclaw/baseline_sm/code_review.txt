CORRECTNESS:  
The function correctly identifies duplicate values in the list and returns them in a list. However, there is a logical flaw: if a value appears multiple times, it will only be added to `dupes` the first time it is encountered (due to the `not in dupes` check), which is the intended behavior. But the condition `lst[i] == lst[j]` only checks for equality between two elements, and the function does not ensure that the value appears at least twice. While the logic is sound in that it adds a value to `dupes` when it's found again, the function may miss duplicates if they are not found in a pairwise comparison that triggers the condition. More importantly, the function will not return duplicates that appear only once — it only returns values that appear at least twice, which is correct. However, the order of duplicates in the output is not guaranteed (e.g., first occurrence of a duplicate may vary), and the function does not guarantee uniqueness in the output — it could potentially add the same value multiple times if the `not in dupes` check fails due to timing or list mutation. But since `lst[i] not in dupes` is checked before appending, the value is added only once. So overall, correctness is acceptable for detecting *distinct* duplicates, though the logic could be simplified and made more robust.

PERFORMANCE:  
The function has a time complexity of O(n²) due to the nested loops, which is inefficient for large lists. For each element, it checks all subsequent elements, leading to redundant comparisons. Additionally, the `lst[i] not in dupes` check inside the inner loop performs a linear search in the `dupes` list, which adds another O(d) cost per check (where d is the number of duplicates found), making the overall complexity worse. For large inputs, this becomes