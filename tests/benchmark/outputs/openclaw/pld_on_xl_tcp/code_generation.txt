I need to write a Python function that reverses a linked list. I'll need to:

1. Define a Node class for the linked list
2. Create a function to reverse the linked list
3. Include helper functions to create and display the list
4. Add a __main__ section with an example

Let me think about the reversal algorithm:
- I'll use the iterative approach with three pointers: prev, current, and next
- For each node, I'll reverse the direction of the pointer
- Keep track of the previous node to point back to it

Let me write clean, well-documented code.


```python
class Node:
    """A node in a singly linked list."""
    def __init__(self, data):
        self.data = data
        self.next = None


def reverse_linked_list(head):
    """
    Reverses a singly linked list iteratively.

    Args:
        head (Node): The head node of the linked list.

    Returns:
        Node: The new head of the reversed linked list.
    """
    prev = None
    current = head

    while current:
        next_node = current.next  # Store next node
        current.next = prev       # Reverse the link
        prev = current            # Move prev forward
        current = next_node       # Move current forward

    return prev


def create_linked_list(values):
    """
    Creates a linked list from a list of values.

    Args:
        values (list): List of values to insert.

    Returns:
        Node: The head of the created linked list.
    """
    if not values:
        return None

    head = Node(values[0])
    current = head

    for value in values[1:]:
        current.next = Node(value)
        current = current.next

    return head


def print_linked_list(head):
    """
    Prints the values of a linked list.

    Args:
        head (Node): The head node of the linked list.
    """
    values = []
    current = head
    while current:
        values.append(str(current.data))
        current = current.next
    print(" -> ".join(values) if values else "Empty list")


if __name__ == "__main__":
    # Create a linked list: 1 -> 