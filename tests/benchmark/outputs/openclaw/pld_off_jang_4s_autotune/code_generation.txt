I need to write a Python function that reverses a linked list. I should include:
1. A Node class definition for the linked list
2. A function to reverse the linked list
3. A short example in __main__ to demonstrate the functionality

Let me think about the approach:
- I'll create a Node class with data and next attributes
- I'll create a LinkedList class or just work with nodes directly
- For reversing, I'll use the iterative approach with three pointers (prev, current, next)
- I'll add helper functions to create and print the list for demonstration

Let me write clean, well-documented code.


```python
class Node:
    """A node in a singly linked list."""
    def __init__(self, data):
        self.data = data
        self.next = None


def reverse_linked_list(head):
    """
    Reverses a singly linked list iteratively.

    Args:
        head (Node): The head node of the linked list.

    Returns:
        Node: The new head of the reversed linked list.
    """
    prev = None
    current = head

    while current:
        next_node = current.next  # Store the next node
        current.next = prev       # Reverse the link
        prev = current            # Move prev forward
        current = next_node       # Move current forward

    return prev  # prev is now the new head


def create_linked_list(values):
    """
    Creates a linked list from a list of values.

    Args:
        values (list): List of values to create nodes from.

    Returns:
        Node: The head node of the created linked list.
    """
    if not values:
        return None

    head = Node(values[0])
    current = head

    for value in values[1:]:
        current.next = Node(value)
        current = current.next

    return head


def print_linked_list(head):
    """
    Prints the values of a linked list.

    Args:
        head (Node): The head node of the linked list.
    """
    values = []
    current = head
    while current:
        values.append(str(current.data))
        current = current.next
    print(" -> ".join(values) if values else "Empty list")


if __name__