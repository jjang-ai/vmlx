The user wants me to write a Python function that reverses a linked list. I need to:

1. Define a Node class for the linked list
2. Create a function to reverse the linked list
3. Include a working example in __main__

Let me think about the implementation:

1. Node class should have value and next pointer
2. To reverse a linked list, I can use either:
   - Iterative approach (more efficient)
   - Recursive approach (more elegant but uses stack space)

I'll use the iterative approach as it's more efficient and commonly used.

The algorithm:
- Keep track of three pointers: prev, current, and next
- Iterate through the list, reversing each node's next pointer
- Update pointers appropriately

Let me write a complete, working

```python
class Node:
    """A node in a singly linked list."""
    def __init__(self, value):
        self.value = value
        self.next = None

def reverse_linked_list(head):
    """
    Reverses a singly linked list iteratively.

    Args:
        head (Node): The head node of the linked list.

    Returns:
        Node: The new head node of the reversed linked list.
    """
    prev = None
    current = head

    while current:
        next_node = current.next  # Store next node
        current.next = prev       # Reverse the link
        prev = current            # Move prev forward
        current = next_node       # Move current forward

    return prev  # New head of the reversed list

def print_linked_list(head):
    """
    Prints the values in the linked list.

    Args:
        head (Node): The head node of the linked list.
    """
    values = []
    current = head
    while current:
        values.append(str(current.value))
        current = current.next
    print(" -> ".join(values) if values else "Empty list")

def create_linked_list(values):
    """
    Creates a linked list from a list of values.

    Args:
        values (list): A list of values to create nodes from.

    Returns:
        Node: The head node of the created linked list.
    """
    if not values:
        return None
    head = Node(values[