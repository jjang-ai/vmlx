The user wants me to create a Python function that reverses a linked list. I need to:

1. Define a Node class for the linked list
2. Create a function to reverse the linked list
3. Include a main section with an example showing how to use it

Let me think about the implementation:
- A linked list node needs a value and a reference to the next node
- To reverse a linked list, I need to change the direction of pointers
- I can do this iteratively or recursively
- I'll provide an iterative solution as it's more efficient and easier to understand

Let me write a complete, working example with:
- Node class definition
- Function to reverse the linked list
- Helper function to print the linked list
- Example in __main__ block


```python
class Node:
    """A node in a singly linked list."""
    def __init__(self, value):
        self.value = value
        self.next = None

def reverse_linked_list(head):
    """
    Reverses a singly linked list iteratively.

    Args:
        head (Node): The head node of the linked list.

    Returns:
        Node: The new head of the reversed linked list.
    """
    prev = None
    current = head

    while current:
        next_node = current.next
        current.next = prev
        prev = current
        current = next_node

    return prev

def print_linked_list(head):
    """
    Prints the values of a linked list.

    Args:
        head (Node): The head node of the linked list.
    """
    values = []
    current = head
    while current:
        values.append(str(current.value))
        current = current.next
    print(" -> ".join(values) if values else "Empty list")

def create_linked_list(values):
    """
    Creates a linked list from a list of values.

    Args:
        values (list): List of values to create nodes from.

    Returns:
        Node: The head node of the created linked list.
    """
    if not values:
        return None
    head = Node(values[0])
    current = head
    for value in values[1:]:
        current.next = Node(value)
        current = current.next
    return head

if __name