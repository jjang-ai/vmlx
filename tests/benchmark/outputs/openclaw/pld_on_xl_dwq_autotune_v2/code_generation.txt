The user wants me to write a Python function that reverses a linked list. I need to:
1. Define a Node class for the linked list
2. Write a function to reverse the linked list
3. Include a main section with an example demonstrating the functionality

Let me create a clean, well-documented solution with:
- Node class with value and next pointer
- A reverse function (I'll use the iterative approach as it's more efficient)
- Helper functions to create and print the list
- A main section showing the before and after states

Let me write this

```python
class Node:
    """Represents a node in a singly linked list."""
    def __init__(self, data):
        self.data = data
        self.next = None


def reverse_linked_list(head):
    """
    Reverses a singly linked list iteratively.

    Args:
        head (Node): The head node of the linked list.

    Returns:
        Node: The new head of the reversed linked list.
    """
    prev = None
    current = head

    while current:
        next_node = current.next
        current.next = prev
        prev = current
        current = next_node

    return prev


def create_linked_list(values):
    """
    Creates a linked list from a list of values.

    Args:
        values (list): List of values to insert into the linked list.

    Returns:
        Node: The head node of the created linked list.
    """
    if not values:
        return None

    head = Node(values[0])
    current = head

    for value in values[1:]:
        current.next = Node(value)
        current = current.next

    return head


def print_linked_list(head):
    """
    Prints the elements of a linked list.

    Args:
        head (Node): The head node of the linked list.
    """
    elements = []
    current = head
    while current:
        elements.append(str(current.data))
        current = current.next
    print(" -> ".join(elements))


if __name__ == "__main__":
    # Example usage
    print("Original linked list:")
    head = create_linked_list([1, 2, 3, 4, 5])
   