The user wants me to write a Python function that reverses a linked list. I need to:

1. Define a Node class for the linked list
2. Create a function to reverse the linked list
3. Include an example in __main__ to demonstrate how it works

Let me create a complete, working example with:
- Node class with value and next pointer
- Function to reverse the linked list (iterative approach is usually better)
- Helper function to print the linked list
- Example usage in __main__

I'll use an iterative approach as it's more efficient (O(1) space complexity

```python
class Node:
    """Node class for singly linked list."""
    def __init__(self, value):
        self.value = value
        self.next = None

def reverse_linked_list(head):
    """
    Reverses a singly linked list iteratively.
    
    Args:
        head (Node): The head node of the linked list.
    
    Returns:
        Node: The new head of the reversed linked list.
    """
    prev = None
    current = head
    
    while current:
        next_node = current.next  # Save the next node
        current.next = prev       # Reverse the link
        prev = current            # Move prev forward
        current = next_node       # Move current forward
    
    return prev  # New head of the reversed list

def print_linked_list(head):
    """
    Prints the linked list values.
    
    Args:
        head (Node): The head node of the linked list.
    """
    values = []
    current = head
    while current:
        values.append(str(current.value))
        current = current.next
    print(" -> ".join(values) if values else "Empty list")

def create_linked_list(values):
    """
    Creates a linked list from a list of values.
    
    Args:
        values (list): List of values to create the linked list from.
    
    Returns:
        Node: The head node of the created linked list.
    """
    if not values:
        return None
    
    head = Node(values[0])
    current = head
    for value in values[1:]:
        current.next = Node(value)
        current = current.next
    return head

if __