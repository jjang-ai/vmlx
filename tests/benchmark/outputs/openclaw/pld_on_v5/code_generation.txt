Let me think about how to approach this coding problem.

Problem: Write a Python function that reverses a linked list. Include the Node class definition and a short example in __main__.

Approach: - `prev`: Points to the previously visited node (initially `None`)
- `curr`: Points to the current node being processed (initially `head`)
- `temp`: Temporarily stores `curr.next` before we modify it

Key considerations:
- Args:
        head: The head node of the linked list to reverse.
    
    Returns:
        The new head node of the reversed linked list.
    
    Time Complexity: O(n) - we traverse the list exactly once.

Let me implement this solution.


<Thinking>
1.  **Understand the Goal**: The user wants a Python script that defines a singly linked list `Node` class and a function to reverse the list. It should also include a demonstration in the `if __name__ == "__main__":` block.

2.  **Define the Data Structure**:
    *   I need a `Node` class.
    *   Attributes: `val` (integer or generic data) and `next` (reference to the next node, initially `None`).

3.  **Design the Reversal Algorithm**:
    *   **Approach**: Iterative approach is generally preferred for its O(1) space complexity compared to the recursive approach's O(n) stack space.
    *   **Pointers**: I need three pointers:
        1.  `prev`: Initially `None`. This will eventually become the new head.
        2.  `curr`: Initially `head`. This traverses the list.
        3.  `next_temp`: A temporary placeholder to store the next node so we don't lose the rest of the list when we reverse the pointer.
    *   **Loop**: While `curr` is not `None`:
        1.  Store `curr.next` in `next_temp`.
        2.  Point `curr.next` to `prev` (the reversal step).
        3.  Move `prev` to `curr`.
        4.  Move `curr` to `next_temp`.
    *   **Termination**: When `curr` is `None`, `prev` is pointing to the last node visited, which is the